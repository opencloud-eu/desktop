when:
  event: push
  branch: stable-1.0

steps:
  release-helper:
    image: woodpeckerci/plugin-ready-release-go:latest
    settings:
      git_email: devops@opencloud.eu
      forge_type: github
      forge_token:
        from_secret: github_token
      release_branch: stable-1.0
